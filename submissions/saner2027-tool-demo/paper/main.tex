\documentclass[10pt,conference]{IEEEtran}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xurl}

\title{Replayable State-Diff Harnesses for Repository-Changing Agents}
\author{\IEEEauthorblockN{Song Luo}
\IEEEauthorblockA{\texttt{luosongred@gmail.com}}}

\begin{document}
\maketitle

\begin{abstract}
Software agents now edit files, run tests, inspect repositories, and prepare
changes that may affect downstream maintenance. This tool demonstration
presents a replayable harness for evaluating whether such agents operate under
explicit state and permission contracts. The artifact models tool calls,
observations, permission gates, memory scope, replay, and final state diffs. It
contains 24 cases, raw run logs, regenerated tables, and validation scripts. For
software analysis and evolution, the key contribution is an inspectable way to
treat an agent trajectory as a maintainable software artifact: what changed,
which tool produced it, whether policy allowed it, and whether the failure can
be replayed. Tool:
\url{https://github.com/rrrrrredy/agent-harness-paper/tree/v0.4.0-saner2027-tool-demo-submission}.
Archive: \url{https://doi.org/10.5281/zenodo.20907471}. Video:
\url{https://github.com/rrrrrredy/agent-harness-paper/releases/download/v0.1.0-preprint/agent-harness-saner-tool-demo-video.mp4}.
\end{abstract}

\section{Problem}

Coding and workflow agents create new maintenance questions. A final answer
does not reveal whether the agent edited the intended file, respected a dry-run
policy, avoided secrets, scoped memory correctly, or left a replayable trace.
These questions are central to software analysis, evolution, and
reengineering because agent behavior becomes part of the evolving development
process.

The problem is not limited to wrong answers. A repository-changing agent can
produce a plausible summary while skipping tests, touching an unrelated file,
using stale memory, accepting an injected instruction from an observation, or
preparing an irreversible side effect without review. These are software
analysis problems because the evidence is distributed across files, commands,
tool calls, policy decisions, and final state.

\section{Tool}

The demonstrated tool is a small replayable harness for stateful agent
contracts. It provides:

\begin{itemize}
\item case definitions with initial state, allowed tools, forbidden tools, and
state assertions;
\item mock tools for file, repository, memory, email/browser-like, and review
state;
\item prompt variants for no harness, thick checklist, and thin contract;
\item metrics for task success, state-diff correctness, invalid tool calls,
permission violations, unsafe secret access, and replayability;
\item scripts for validation, regeneration, source evidence audit, and secret
scanning.
\end{itemize}

\begin{table}[t]
\caption{Tool Inputs and Generated Outputs}
\centering
\begin{tabular}{p{0.26\linewidth}p{0.58\linewidth}}
\toprule
Input or output & Role in the tool \\
\midrule
Case JSONL & Defines task, initial state, allowed tools, forbidden tools, and assertions \\
Action trace & Captures tool names, arguments, observations, and replay evidence \\
State diff & Shows whether files, memory, reports, and side effects changed correctly \\
Policy result & Reports invalid calls, permission violations, and secret access \\
Fixture suite & Materializes selected cases as repository before/after evidence \\
Tables & Summarize success, failure taxonomy, uncertainty, and paired deltas \\
\bottomrule
\end{tabular}
\end{table}

This differs from a conventional LLM evaluation script. The unit under
inspection is not only a generated answer, but an execution record. The tool
keeps the answer connected to the actions that produced it and to the final
state those actions changed. That connection is what makes the artifact useful
for software maintenance: a maintainer can ask whether a patch was produced by
the expected tool, whether tests ran, whether policy allowed the action, and
whether the failure can be reproduced.

\section{Architecture}

The tool has four layers. The \emph{case layer} defines task intent, initial
state, allowed tools, forbidden tools, required tools, and final-state
assertions. The \emph{execution layer} normalizes model outputs into tool
actions and applies those actions to a mock state containing files, memory,
reports, email-like side effects, browser state, and test status. The
\emph{oracle layer} checks state assertions and policy outcomes. The
\emph{analysis layer} regenerates summary tables, failure taxonomies,
uncertainty summaries, and fixture traces from raw JSONL rows.

The repository also contains a small concrete repository fixture. It maps one
stateful tool-use case to a before/after tree: \texttt{src/app.py} begins with
\texttt{VALUE = 1}, the contract trace writes \texttt{VALUE = 2}, and a
no-dependency test checks the final value. The generated output includes a
unified diff, file hashes, and a JSON trace. This fixture is intentionally
small so reviewers can inspect the full state transition.

\begin{table}[t]
\caption{Repository Fixture Scenarios}
\centering
\begin{tabular}{p{0.29\linewidth}p{0.16\linewidth}p{0.37\linewidth}}
\toprule
Scenario & Outcome & Evidence exposed \\
\midrule
Correct update & Pass & Target diff, hashes, test evidence \\
Missing tests & Fail & Expected patch exists but validation is absent \\
Wrong file & Fail & Unrelated diff and failing validation \\
Secret read & Fail & Forbidden access and policy violation \\
\bottomrule
\end{tabular}
\end{table}

\section{Demo}

The demo walks through a repository-changing scenario:

\begin{enumerate}
\item load a case that requires a safe repository update;
\item show the expected tool contract and forbidden actions;
\item run the harness and inspect the resulting action trace;
\item compare initial and final mock state;
\item inspect a failure where a broad prompt checklist does not enforce the
contract;
\item regenerate aggregate tables from raw runs.
\end{enumerate}

The artifact is designed for reviewers to execute without live model keys. Live
provider runs are included as raw rows; deterministic validation checks the
simulator and metrics.

The recommended reviewer path is:

\begin{enumerate}
\item run \texttt{python scripts/run\_checks.py --mode validate};
\item run \texttt{python scripts/run\_demo\_walkthrough.py};
\item run \texttt{python scripts/run\_repo\_fixture\_demo.py};
\item inspect \texttt{results/fixtures/saner\_repo\_fixture.diff};
\item inspect \texttt{results/fixtures/saner\_repo\_fixture\_suite.md};
\item inspect the failure taxonomy under \texttt{results/tables/}.
\end{enumerate}

This path exercises the tool without requiring provider credentials. Reviewers
who want to reproduce live API runs can supply provider keys, but the
submission does not depend on those keys.

\section{Demonstration Storyboard}

The screencast is organized around the questions a maintainer would ask before
trusting an agent-generated change.

\textbf{Step 1: inspect the contract.} The demo opens a case definition and
shows the task, allowed tools, forbidden tools, required actions, and final
state assertions. This makes the expected behavior explicit before any model
output is considered.

\textbf{Step 2: run the no-key reviewer path.} The demo runs the validation and
walkthrough scripts. The walkthrough loads all 24 cases, evaluates a security
case, regenerates local reference rows, and rebuilds aggregate tables. This
demonstrates that the artifact is reviewable without live provider access.

\textbf{Step 3: inspect repository-state evidence.} The demo runs the
repository fixture suite and opens the generated diff, trace, and fixture-suite
table. The passing scenario shows the expected patch and validation evidence.
The negative scenarios show missing tests, a wrong-file mutation, and a secret
read. These cases demonstrate why the tool evaluates state and policy rather
than final answers alone.

\textbf{Step 4: inspect aggregate failures.} The demo opens the regenerated
failure taxonomy and summary tables. The goal is not to rank providers, but to
show how operational failures become categories that a maintainer can act on:
state-diff errors, permission violations, invalid tool calls, unsafe secret
access, routing errors, parse failures, and recovery failures.

\begin{table}[t]
\caption{Reviewer Questions Supported by the Tool}
\centering
\begin{tabular}{p{0.34\linewidth}p{0.50\linewidth}}
\toprule
Reviewer question & Artifact evidence \\
\midrule
Did the agent change the intended file? & Unified diff and state assertions \\
Did it run validation before claiming success? & Test-status trace and fixture result \\
Did it cross a forbidden boundary? & Permission and secret-access metrics \\
Can the failure be diagnosed later? & Action trace, observations, replay metadata \\
Can results be regenerated? & No-key scripts and raw JSONL rows \\
\bottomrule
\end{tabular}
\end{table}

\section{Artifact Evaluation}

The tool-demo artifact is evaluated along four practical criteria.

\textbf{Reproducibility.} The reviewer path runs without provider keys. The
validation script checks tests, public-trace hygiene, release references,
archive manifests, source evidence, and secret scanning. The analysis scripts
rebuild tables from raw rows rather than relying on manually edited numbers.

\textbf{Inspectability.} Each run preserves the action list, final mock state,
notes, metric values, and raw response metadata where available. For repository
fixtures, the tool also emits a diff and file hashes. This makes the execution
auditable by a software maintainer, not only by an LLM evaluator.

\textbf{Boundary coverage.} The current case set covers trigger/routing,
stateful tools, permission and injection behavior, memory scoping, and
replay/recovery. The fixture suite adds concrete repository evidence for one
success case and three negative cases.

\textbf{Extensibility.} New cases can be added as JSONL records with initial
state, expected tools, forbidden tools, and assertions. New provider rows can
be recorded without changing the analysis scripts. New repository fixtures can
be added by materializing case state into before/after trees.

\begin{table}[t]
\caption{Artifact Evaluation Summary}
\centering
\begin{tabular}{p{0.28\linewidth}p{0.18\linewidth}p{0.38\linewidth}}
\toprule
Criterion & Status & Evidence \\
\midrule
No-key execution & Supported & Validation, walkthrough, local reference runner \\
State-diff evidence & Supported & Fixture diff, hashes, final-state assertions \\
Failure taxonomy & Supported & Regenerated tables from raw rows \\
Provider reruns & Optional & Live runner requires user-supplied API keys \\
Large repo scale & Future work & Current fixtures are intentionally small \\
\bottomrule
\end{tabular}
\end{table}

\section{Relevance to SANER}

The harness treats agent trajectories as analyzable software artifacts. It can
support regression testing of agent workflows, analysis of repository side
effects, evolution of prompt and tool schemas, and reengineering of brittle
agent scripts into explicit contracts. This fits SANER's focus on extracting
useful information from software and system artifacts and using that information
for maintenance and renewal.

The tool is particularly useful for three maintenance workflows.

\textbf{Regression testing agent configurations.} When a model, prompt, or tool
schema changes, teams can rerun the same cases and compare state-diff failures,
invalid tool calls, permission violations, and replayability.

\textbf{Auditing repository side effects.} A reviewer can inspect which file
changed, which tool produced the change, which policy allowed it, and whether
the expected validation ran.

\textbf{Reengineering prompt scripts.} Existing agent workflows often rely on
long checklists. The harness helps migrate those scripts toward explicit
contracts: allowed tools, forbidden effects, state assertions, and review
gates.

\section{Adoption Scenario}

A team maintaining a coding-agent workflow can use the tool as a regression
suite. Before upgrading a model or changing a tool schema, the team records a
small set of contracts that represent its repository policies: which files may
be changed, which validation commands must run, which side effects require
review, and which paths are forbidden. After the upgrade, the same cases are
rerun. A changed final answer is not enough to fail or pass the upgrade; the
team inspects the state-diff and policy outcomes.

This adoption scenario is deliberately lightweight. The harness does not
replace the agent runtime. It provides a boundary-oriented evaluation layer
around the runtime. That is the central design choice: keep the harness thin
around model cognition, but strong around external effects that software
engineers must review and maintain.

\section{Limitations}

The current artifact is a pilot tool, not a large benchmark. Most cases use
mock state rather than full repositories. The concrete repository fixture is
small. The provider rows are descriptive and should not be interpreted as a
general model ranking. The demonstration value is the analysis shape: final
answers are connected to tool traces, state diffs, policy outcomes, and replay
records.

\section{Availability}

The repository and release archive are public. Code is MIT licensed; manuscript,
benchmark cases, evidence snapshots, tables, figures, and documentation are CC
BY 4.0.

\begin{thebibliography}{9}
\bibitem{toolsandbox} J. Lu et al., ``ToolSandbox: A Stateful, Conversational,
Interactive Evaluation Benchmark for LLM Tool Use Capabilities,'' Findings of
ACL: NAACL, 2025.
\bibitem{agentdojo} E. Debenedetti et al., ``AgentDojo: A Dynamic Environment
to Evaluate Prompt Injection Attacks and Defenses for LLM Agents,'' NeurIPS
Datasets and Benchmarks, 2024.
\bibitem{sweagent} J. Yang et al., ``SWE-agent: Agent-Computer Interfaces
Enable Automated Software Engineering,'' arXiv:2405.15793, 2024.
\bibitem{deli} D. Chen, ``Deli AutoResearch: Autonomous Research Framework,''
2026.
\end{thebibliography}

\end{document}
