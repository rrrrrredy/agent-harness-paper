[project]
name = "agent-harness-paper"
version = "0.1.0"
description = "Paper artifacts and pilot harness for production-oriented agent harnesses"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
